# Perkley environment variables
#
# Copy app-specific examples — never commit real secrets:
#   cp apps/api/.env.example apps/api/.env
#   cp apps/web/.env.example apps/web/.env.local
#
# Use separate Supabase projects and Razorpay accounts per environment:
#   development | staging | production
#
# Never run payment tests against live Razorpay keys.

# See apps/api/.env.example for backend variables
# See apps/web/.env.example for frontend variables
