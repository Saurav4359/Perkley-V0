{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@perkley/database": "workspace:*",
    "@perkley/email": "workspace:*",
    "@perkley/validations": "workspace:*",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@perkley/typescript-config": "workspace:*",
    "@types/bun": "^1.3.3",
    "typescript": "^5"
  }
}
